{
  "name": "Sage Theme Development",
  "image": "mcr.microsoft.com/devcontainers/php:8.4",

  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "24"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "shufo.vscode-blade-formatter",
        "open-southeners.laravel-pint"
      ]
    }
  },

  "forwardPorts": [5173],

  "postCreateCommand": "curl -fsSL https://cli.kiro.dev/install | bash && composer --version && php --version && node --version && kiro-cli --version"
}
